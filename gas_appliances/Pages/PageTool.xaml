<Page x:Class="gas_appliances.Pages.PageTool"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:gas_appliances.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageTool">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Name="menuAddTool" Header="Добавить" 
                      Click="menuAddTool_Click"/>
            <!--<MenuItem Name="menuReport" Header="Отчет"
                      Click="menuReport_Clic"/>-->
            <MenuItem Name="menuLogOut" Header="Выход"
                      Click="menuLogOut_Click"/>
        </Menu>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1" 
                    VerticalAlignment="Center">
            <Label Content="Поиск:"/>
            <TextBox Width="150" Height="25" Margin="10, 0"
                     Name="txbSearchTools" 
                     TextChanged="txbSearchTools_TextChanged"/>
            <ComboBox Height="25" Width="150" Name="cmbCategory"
                      SelectionChanged="cmbCategory_SelectionChanged"/>
            <Button Height="25" Width="75" Name="btnRefresh"
                    Content="Обновить" Click="btnRefresh_Click" Margin="10,0"/>
        </StackPanel>
        
        <DataGrid Name="dgrTools" Grid.Row="2" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  MouseDoubleClick="dgrTools_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Код" Binding="{Binding Id}"
                                    Width="auto"/>
                <DataGridTemplateColumn Header="Категория" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock DataContext="{Binding Path=ToolCategory}">
                                <TextBlock.Text>
                                    <Binding Path="ToolCategoryName"/>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Модель" Binding="{Binding ModelName}"
                                    Width="*"/>
                <DataGridTextColumn Header="В экспл." Binding="{Binding OperatingSince, StringFormat=d}" 
                                    Width="auto"/>
                <DataGridTextColumn Header="След. поверка" Binding="{Binding NextExamination, StringFormat=d}" 
                                    Width="auto"/>
                <DataGridTextColumn Header="Списан" Binding="{Binding DecomissionedSince, StringFormat=d}"
                                    Width="auto"/>
                <DataGridTemplateColumn Header="Производитель" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock DataContext="{Binding Path=ToolManufacturer}">
                                <Run DataContext="{Binding Path=ToolManufacturerType}"
                                     Text="{Binding ManufacturerTypeName}"/>
                                <Run Text="{Binding ManufacturerName, StringFormat='&quot;{0}&quot;'}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Серийный номер" 
                                    Binding="{Binding SerialNumber}" Width="auto"/>
                <DataGridTextColumn Header="Заметки" Binding="{Binding Notes}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
